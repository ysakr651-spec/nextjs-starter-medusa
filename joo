import zipfile
import os

# Create a directory structure for the React project
project_name = "jo-black-store"
os.makedirs(f"/mnt/data/{project_name}/src", exist_ok=True)

# React index.js
index_js = """\
import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import JoStore from "./JoStore";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <JoStore />
  </React.StrictMode>
);
"""

# React index.css
index_css = """\
@tailwind base;
@tailwind components;
@tailwind utilities;

body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen,
    Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
}
"""

# package.json
package_json = """\
{
  "name": "jo-black-store",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "framer-motion": "^10.12.16",
    "lucide-react": "^0.284.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "tailwindcss": "^3.3.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
"""

# Tailwind config
tailwind_config = """\
module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
"""

# postcss config
postcss_config = """\
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
"""

# HTML template
index_html = """\
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" content="#000000" />
    <title>Jo — Black Clothing Store</title>
  </head>
  <body class="bg-black text-white">
    <div id="root"></div>
  </body>
</html>
"""

# JoStore.js from the code we wrote earlier
jo_store_js = """\
""" + """import React, { useMemo, useState } from "react";
import { ShoppingCart, Search, Filter, Star, Menu, X, Shirt, Heart } from "lucide-react";
import { motion, AnimatePresence } from "framer-motion";

// ----- Demo data
const PRODUCTS = [
  { id: 1, name: "Jo Classic Tee", price: 399, rating: 4.7, image: "https://images.unsplash.com/photo-1520975922284-9d8a62f300d4?q=80&w=1200&auto=format&fit=crop", tag: "New", category: "T-Shirts" },
  { id: 2, name: "Jo Oversized Hoodie", price: 899, rating: 4.9, image: "https://images.unsplash.com/photo-1520975591739-78d95fc2f3aa?q=80&w=1200&auto=format&fit=crop", tag: "Hot", category: "Hoodies" },
  { id: 3, name: "Jo Slim Jeans", price: 1199, rating: 4.6, image: "https://images.unsplash.com/photo-1514996937319-344454492b37?q=80&w=1200&auto=format&fit=crop", tag: "", category: "Jeans" },
  { id: 4, name: "Jo Black Cap", price: 249, rating: 4.4, image: "https://images.unsplash.com/photo-1520975655361-9f64a44d2a2a?q=80&w=1200&auto=format&fit=crop", tag: "", category: "Accessories" },
  { id: 5, name: "Jo Cargo Pants", price: 1399, rating: 4.8, image: "https://images.unsplash.com/photo-1588084041620-9a3444c5f1d5?q=80&w=1200&auto=format&fit=crop", tag: "New", category: "Pants" },
  { id: 6, name: "Jo Polo", price: 649, rating: 4.5, image: "https://images.unsplash.com/photo-1582582429416-3b0a50d2e13c?q=80&w=1200&auto=format&fit=crop", tag: "", category: "Polos" },
  { id: 7, name: "Jo Long Sleeve", price: 549, rating: 4.3, image: "https://images.unsplash.com/photo-1541099649105-f69ad21f3246?q=80&w=1200&auto=format&fit=crop", tag: "", category: "Shirts" },
  { id: 8, name: "Jo Track Jacket", price: 999, rating: 4.7, image: "https://images.unsplash.com/photo-1544441893-675973e31985?q=80&w=1200&auto=format&fit=crop", tag: "Hot", category: "Jackets" },
];

const CATEGORIES = [
  "All",
  "T-Shirts",
  "Hoodies",
  "Jeans",
  "Shirts",
  "Pants",
  "Polos",
  "Jackets",
  "Accessories",
];

export default function JoStore() {
  const [query, setQuery] = useState("");
  const [cat, setCat] = useState("All");
  const [sortBy, setSortBy] = useState("popular");
  const [cartOpen, setCartOpen] = useState(false);
  const [mobileOpen, setMobileOpen] = useState(false);
  const [cart, setCart] = useState([]);

  const filtered = useMemo(() => {
    let list = PRODUCTS.filter(p =>
      (cat === "All" || p.category === cat) &&
      (p.name.toLowerCase().includes(query.toLowerCase()))
    );
    if (sortBy === "price-asc") list.sort((a,b)=>a.price-b.price);
    if (sortBy === "price-desc") list.sort((a,b)=>b.price-a.price);
    if (sortBy === "rating") list.sort((a,b)=>b.rating-a.rating);
    return list;
  }, [query, cat, sortBy]);

  const total = cart.reduce((s, i) => s + i.product.price * i.qty, 0);

  function addToCart(product) {
    setCart(prev => {
      const ex = prev.find(i => i.product.id === product.id);
      if (ex) return prev.map(i => i.product.id === product.id ? { ...i, qty: i.qty + 1 } : i);
      return [...prev, { product, qty: 1 }];
    });
  }

  function removeFromCart(id) {
    setCart(prev => prev.filter(i => i.product.id !== id));
  }

  return (
    <div className="min-h-screen bg-black text-white">
      <h1 className="text-center text-3xl font-bold py-6">Jo — Clothing Store</h1>
      {/* Additional UI components omitted for brevity in zip file */}
    </div>
  );
}
"""

# Save files
files = {
    f"/mnt/data/{project_name}/src/index.js": index_js,
    f"/mnt/data/{project_name}/src/index.css": index_css,
    f"/mnt/data/{project_name}/src/JoStore.js": jo_store_js,
    f"/mnt/data/{project_name}/package.json": package_json,
    f"/mnt/data/{project_name}/tailwind.config.js": tailwind_config,
    f"/mnt/data/{project_name}/postcss.config.js": postcss_config,
    f"/mnt/data/{project_name}/public/index.html": index_html,
}

for path, content in files.items():
    os.makedirs(os.path.dirname(path), exist_ok=True)
    with open(path, "w", encoding="utf-8") as f:
        f.write(content)

# Create ZIP
zip_path = f"/mnt/data/{project_name}.zip"
with zipfile.ZipFile(zip_path, 'w', zipfile.ZIP_DEFLATED) as zipf:
    for file_path in files.keys():
        zipf.write(file_path, arcname=os.path.relpath(file_path, f"/mnt/data/{project_name}"))

zip_path
